# Ralph Progress Log
Started: 2026-06-16 (Fase 1 — Multi-Tribunal / TJSP)
Histórico da run anterior (scraper TJSC, US-001..US-004): archive/2026-06-16-scraper-batch-nightly/progress.txt
---

## Codebase Patterns
- Scraper usa requests.Session com headers HEADERS definidos no topo do módulo
- Parsing HTML usa BeautifulSoup com seletor div.resultadoItem para cada card
- Payload do POST (fetch_page) é idêntico entre tribunais — só mudam a URL (base eproc) e os headers Origin/Referer
- Campos vêm em pares .resLabel / .resValue; no TJSC ficam em div.row, no TJSP/TJRJ vários ficam em <div class="col-12 col-md-4"> — por isso o parser deve iterar .resLabel direto (não só div.row)
- extract_processo usa a.numero-processo como prioridade, fallback para campo PROCESSO (split()[0]) — já limpa lixo tipo "ProCom - Procedimento" do TJSP
- Relator: label varia — RELATOR/RELATORA (TJSC, TJRJ) e MAGISTRADA/MAGISTRADO (TJSP)
- Variáveis de ambiente GCP: GCP_PROJECT_ID, BQ_DATASET_ID, BQ_TABLE_ID (=processos_raw nesta fase, dados BRUTOS sem embedding)
- ID de registro: SHA-256 de "{processo}|{data_julgamento}" — número CNJ já discrimina o tribunal (.8.26.=SP, .8.24.=SC, .8.19.=RJ)
- BigQuery insert: chunks de 256 com retry 3x; mock em teste: patch("screping.bq_writer.bigquery.Client")
- checkpoint.json fica na raiz do repo (não dentro de screping/)
- Usar uv para venv/deps: `uv venv .venv && uv pip install -r requirements.txt --python .venv/bin/python` (não há pip/pip3 no sistema)
- scrape_month aceita failed_pages: list | None = None para coletar falhas sem alterar tipo de retorno
- Padrão de teste de scrape_month: patch("screping.scraper.fetch_page") + patch("time.sleep"); sleep=0 evita lentidão
- 3 páginas consecutivas sem resultado (ou com falha) encerram o mês
- Tribunais drop-in confirmados na Fase 0: TJSC, TJSP (eproc1g.tjsp.jus.br), TJRJ (eproc1g.tjrj.jus.br). TJRS/TJMG/etc usam outra implementação eproc (lançam exceção)
- Embedding de produção (fora desta fase): intfloat/multilingual-e5-base, prefixo "passage: {text}"
---

## 2026-06-16 - US-101
- Refatorado extract_field(card, *labels): itera card.select(".resLabel") direto (não mais div.row); aceita N labels; .resValue par via find_next_sibling, fallback lab.parent.select_one(".resValue")
- parse_results_page: relator com aliases (RELATOR/RELATORA/MAGISTRADA/MAGISTRADO); decisao = DECISÃO or EMENTA
- Arquivos: screping/scraper.py, tests/test_scraper.py (fixtures TJSC/TJSP/TJRJ + testes de alias)
- **Aprendizados:**
  - extract_field agora é variádico (*labels) — assinatura antiga de 1 label continua funcionando posicionalmente
  - find_next_sibling(class_="resValue") cobre TJSC (div.row) e TJSP/TJRJ (col-12 col-md-4) pois label/value são irmãos no mesmo wrapper
  - Pré-existentes FALHANDO antes desta story (fora do escopo US-101): tests/test_checkpoint.py::test_load_checkpoint_missing_file (será resolvido na US-103) e tests/test_embedder.py::test_generate_embeddings_uses_correct_encode_params (embedder fora desta fase). Verificado com git stash que falham sem minhas mudanças.
  - Suite scraper: 17 passed; full: 28 passed / 2 pré-existentes failed
---

## 2026-06-16 - US-102
- Adicionado registro TRIBUNAIS = {tjsc, tjsp, tjrj} com base eproc de cada um
- _urls(tribunal) deriva (LISTAR_RESULTADOS, PAGINAR) do base; levanta ValueError se tribunal inválido
- _headers(tribunal) deriva Origin/Referer do base (removidas as constantes globais BASE/LISTAR_RESULTADOS/PAGINAR/HEADERS fixas no TJSC)
- fetch_page(..., tribunal="tjsc") e scrape_month(..., tribunal="tjsc"); scrape_month injeta record["tribunal"]=tribunal e valida o tribunal cedo via _urls()
- Arquivos: screping/scraper.py, tests/test_scraper.py
- **Aprendizados:**
  - scrape_month chama fetch_page com tribunal como KWARG → os side_effects dos testes existentes precisaram ganhar param tribunal="tjsc" (atualizados 4 side_effects, sem mudar asserts)
  - Default tribunal="tjsc" em todas as assinaturas preserva 100% da compatibilidade (testes antigos de datas/retry/failed_pages passam sem mudança de chamada)
  - Validação de tribunal centralizada em _urls() — fetch_page e scrape_month reaproveitam, evitando duplicar a checagem
  - main.py NÃO referenciava as constantes removidas (confirmado por grep) — sem regressão fora do scraper
  - Suite scraper: 27 passed; full: 38 passed / 2 pré-existentes failed (checkpoint US-103, embedder fora de fase)
---

## 2026-06-16 - US-103
- checkpoint.py: estrutura indexada por tribunal. load_checkpoint(tribunal, path) e save_checkpoint(tribunal, data, path) com nova assinatura
- _load_all(path) centraliza leitura + migração: formato flat antigo ({'next_month': ...}) vira {'tjsc': <conteúdo>}
- save_checkpoint faz merge (lê tudo, atualiza só a chave do tribunal, regrava) — não apaga outros tribunais
- main.py: env TRIBUNAL (default tjsc), env STOP_MONTH (default via DEFAULT_STOP={'tjsc':'2018-01','tjsp':'2024-01','tjrj':'2024-01'}); para quando next_month < STOP_MONTH
- Arquivos: screping/checkpoint.py, screping/main.py, tests/test_checkpoint.py
- **Aprendizados:**
  - A migração flat→tjsc é detectada por 'next_month' in data no topo do dict; per-tribunal nunca tem essa chave no topo
  - _default() usa mês atual (não 2018-01) — o teste antigo test_load_checkpoint_missing_file (que esperava 2018-01) era stale e foi reescrito; pré-existente falho resolvido por esta story
  - Reescrevi test_checkpoint.py inteiro para a nova assinatura (tribunal como 1º arg em load/save)
  - Restante falho agora: só test_embedder (fora desta fase). Suite checkpoint: 7 passed; full: 42 passed / 1 pré-existente failed (embedder)
---
